name: Apply SPE-2772 persistence fixtures

on:
  push:
    branches:
      - jamesdyedbq/spe-2772-canonical-live-facility-workshop-safety-integration

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: jamesdyedbq/spe-2772-canonical-live-facility-workshop-safety-integration
          fetch-depth: 0
      - run: python .github/scripts/apply-spe-2772-persistence-fixtures.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/spe-2772-persistence-fixtures.yml
          git rm .github/scripts/apply-spe-2772-persistence-fixtures.py
          git add src/test/departmentWorkshopPersistence.test.ts
          git commit -m "test(workshops): activate authored facility in persistence fixtures"
          git push origin HEAD:jamesdyedbq/spe-2772-canonical-live-facility-workshop-safety-integration

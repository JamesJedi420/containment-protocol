# Logs
# cspell:ignore vitest gamestore ntvs njsproj
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
coverage
dist-ssr
*.local
*test-results*.json
*test-results*.txt
test_output.txt
vitest-output.txt
agents-test-output.txt
agents-filter-test.txt
flaky_errors.txt
gamestore_full_output.txt
remaining-subset.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
*.code-workspace
.idea

# Cursor-local agent config (solo dev; environment.json stays tracked)
.cursor/skills/
# Shared repo rules are tracked explicitly; other files here stay local-only
.cursor/rules/*
!.cursor/rules/
!.cursor/rules/linear-always-update.mdc
!.cursor/rules/implementation-lite.mdc
!.cursor/rules/agent-cursor-plugins.mdc
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env
.env.*
!.env.example
linear-export/.env

# Linear / GitHub backlog tooling — local-only working scripts, data dumps,
# and notes used by the maintenance agent. Nothing in this folder is intended
# to ship to the repository.
linear-export/

# Loose coverage / test output dumps at the repo root
coverage_out.txt
coverage_out_*.txt

# Local temp / sync staging (agent scripts, Google Drive upload fragments)
.tmp/
.tmp.driveupload/

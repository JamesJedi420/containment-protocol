## Linear

- **Slice issue:** [SPE-2849](https://linear.app/spectranoir/issue/SPE-2849/fabricated-lot-combat-stim-instance-materialization-provenance)
- **Parent issue (if any):** [SPE-2827](https://linear.app/spectranoir/issue/SPE-2827/generic-ordinary-equipment-instance-authority)
- **Child issues covered:** slice only

## Summary

@coderabbitai summary

## What shipped

- Combat Stim production recipe (`combat-stims`) so fabricated lots can exist in play
- `materializeStoredCombatStimInstance` for catalog or fabricated-lot sources (inventory −1; lot path tracked +1, quantity immutable; 2/2 payload + `fabricationOrigin` on lot path)
- Narrow gate lifts for Combat Stim payload + matching lot origin (instantiate, hydration, events); SPE-2845 catalog re-agg fails closed when provenance is present
- Equipment Track UI includes Combat Stim catalog/lot sources; gray-market procurement packet preserved for Combat Stims

## Docs updated

- `planning/spe-2849-fabricated-combat-stim-lot-materialization-slice.md`
- `planning/spe-2846-…` deferred pointer; `planning/backlog.md` + handoff manifest; `SCHEMA_REGISTRY.md`

## Parent status

- Parent SPE-2827 remains **Backlog** — child slice only

## Scope boundary

- No damaged return, fabricated Combat Stim return-to-lot, partial-dose re-agg, generic non-Combat payload, or Auto-Scrap instance selection
- Combat Stim recovery remains instance-only; equip-from-aggregate stays catalog-origin

## Validation

- [x] Targeted tests: combatStim / equipmentView / equipmentInstance / market / fabrication / grade fabrication contracts
- [x] Lint / verify: `npm run lint`, `npm run verify:backlog-handoff`, full `npm run test:run` (8113 passed)

## Follow-ups

- Fabricated Combat Stim return-to-lot (SPE-2827 child)
- Damaged-instance return (SPE-877)

## Linear updated

- [x] Slice issue linked in PR body
- [ ] PR URL on slice issue
- [ ] Post-merge comment on slice issue (what shipped + validation)
